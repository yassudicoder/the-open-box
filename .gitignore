# build output
dist/
.output/

# dependencies
node_modules/

# astro
.astro/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# environment — every .env variant, so a local key can never be committed.
# Real values live in Vercel → Settings → Environment Variables, never in the repo.
.env*

# os
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
